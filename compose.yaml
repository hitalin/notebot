services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      # 未指定なら notecli の先頭アカウント
      NOTEBOT_ACCOUNT: ${NOTEBOT_ACCOUNT:-}
      RUST_LOG: ${RUST_LOG:-notebot=info,notecli=warn}
    volumes:
      - notebot-data:/data

volumes:
  notebot-data:
